<nav>
    <ul>
        {% if app.user %}
            {% if path('sorties_home') and is_granted("ROLE_ADMIN") %}
                <li><a href="{{ path('admin_nav')}}" title="Navigation Admin">Admin</a></li>
            {% endif %}
            <li><a href="{{ path('sorties_home') }}" title="Sorties">Accueil</a></li>
            <li><a href="{{ path('update_profil') }}" title="Mon profil">Mon profil</a></li>
            <li><a href="{{ path('app_logout') }}" title="Déconnexion">Déconnexion ({{ app.user.firstname }})</a></li>
        {% else %}
            <li><a href="{{ path('app_login') }}" title="Connexion">Connexion</a></li>
            <li><a href="{{ path('app_register') }}" title="Créer un compte">Créer un compte</a></li>
        {% endif %}
    </ul>
</nav>