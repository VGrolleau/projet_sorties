{% extends 'base.html.twig' %}

{% block title %}Profil de {{ user.firstname }} {{ user.lastname }}{% endblock %}

{% block body %}
    <h2>Profil de {{ user.firstname }} {{ user.lastname }}</h2>
    <div>Campus : {{ user.school.name }}</div>
    <!--<div id="profile_pic">
        {% if user.picture %}
            <img src="{{ asset('uploads/profiles/'~user.picture) }}" alt="{{ user.firstname }} {{ user.lastname }}">
        {% else %}
            <img src="{{ asset('img/profile-default.jpg') }}" alt="Photo par défaut">
        {% endif %}
    </div>-->

    {% if app.user == user %}
        <a href="{{ path('update_profil') }}">Modifier mon profil</a><br>
        <a href="{{ path('') }}">Modifier mon image</a><br>
        <a href="{{ path('') }}">Modifier mon mot de passe</a>
    {% endif %}
{% endblock %}

