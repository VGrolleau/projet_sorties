<div class="list">
    <div class="list-name">
        <h2>{{ event.name }}</h2>
    </div>
    <div class="list-infos">
        {{ event.duration }}
        {{ event.registrationLimitDate }}
        {{ event.maxRegistrations }}
        {{ event.eventState }}
        {{ event.organizer }}
    </div>
</div>